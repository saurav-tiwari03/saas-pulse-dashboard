# ===========================================
# EXAMPLE ENVIRONMENT FILE
# Copy this to .env (development) or .env.prod (production)
# NEVER commit .env or .env.prod files!
# ===========================================

# Environment
NODE_ENV=development

# Server
PORT=4000

# Database - PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:your-password@localhost:5432/your-database?schema=public"

# Swagger API Documentation (optional in development)
# In production, set SWAGGER_ENABLED=true and configure credentials
SWAGGER_ENABLED=true
SWAGGER_USERNAME=admin
SWAGGER_PASSWORD=change-this-password
